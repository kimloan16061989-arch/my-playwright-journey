const baseURL = 'https://practice.automationexercise.com';
const username = 'testuser@email.com';
const password = 'Test@1234';
let loginStatus = false;// Biến để kiểm tra trạng thái đăng nhập
console.log(`Đăng nhập với username: ${username}, URL: ${baseURL}`);
loginStatus = true;
console.log(`Trạng thái login: ${loginStatus}`);